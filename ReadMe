The main tool, *filter* sits in bin/ compiled on a Mac OS X. 
To compile:
    make
    make clean
This will create binaries for testing, and the tool *filter*.

Example usage of the tool:

bin/filter -i infoID -o opr_to_compare -v againstValue -f sourceFilePath > outfilePath

values allowed for option -o (opr_to_compare)
    smaller
    equal
    larger
